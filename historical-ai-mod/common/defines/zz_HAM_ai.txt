NAI = {
	UNIFICATION_SUPPORT_INVALID_TARGET_FACTOR = -1000 # Unification support factor when country isn't a valid target for the unification play
	UNIFICATION_SUPPORTER_DEFAULT_RANK_VALUE = 2 # 4 # Rank value is compared to this value for computing supporter rank factor

	DIPLO_PLAY_SWAY_CALL_IN_OBLIGATION = 100 # When calling in an obligation
	DIPLO_PLAY_SWAY_MINIMUM_OBLIGATION_VALUE = 20 # 10 # Obligations worth less than this are rejected

	WAR_GOAL_MAKE_PRIMARY_DEMAND_SCORE_MULT = 0.25 # 0.75   # Multiply score of an existing wargoal by this when determining whether to make it into a primary demand instead of adding a new wargoal
	WAR_GOAL_MAKE_PRIMARY_DEMAND_RETURN_STATE_SCORE_MULT = 0.50 # 1.25 # Overrides WAR_GOAL_MAKE_PRIMARY_DEMAND_SCORE_MULT for return state wargoals

	# Below data is used to determine which states should be colonized first
	NUM_GROWING_COLONIES_BASE = 1 # How many colonies will the AI keep growing at once (base amount)
	NUM_GROWING_COLONIES_SCALED = 0.02 # 0.01 # 1 extra colony at once per this amount of colonial growth points
	NUM_GROWING_COLONIES_MAX = 10 # 5 # No more than this amount of growing colonies at once, regardless of amount of colonial growth points
}
